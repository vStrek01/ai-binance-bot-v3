"""Example-only credential placeholder.

DO NOT IMPORT OR EXECUTE THIS FILE.

The trading bot loads credentials from environment variables or a dedicated secrets
manager. This stub exists purely so new contributors know which variables to set:

- BINANCE_API_KEY / BINANCE_API_SECRET
- BINANCE_TESTNET_API_KEY / BINANCE_TESTNET_API_SECRET
- BOT_CONFIRM_LIVE (must be YES_I_UNDERSTAND_THE_RISK for live orders)

Store the real values in a secure secret store or `.env` that never gets committed.
"""
